#include "hsp3cl.as"
#include "hspcurl.as"
#include "htmlparser.as"

creattmp tmpfile
friend_ip = "127.0.0.1"
friend_port = 3000
url = friend_ip + ":" + friend_port
curl url, tmpfile
notesel buf
noteload tmpfile

htmltagn buf, "p", 0, 2, buf
mes buf
