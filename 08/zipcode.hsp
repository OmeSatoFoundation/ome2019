#include "hsp3cl.as"
#include "cmdexec.as"


;	郵便番号から住所を表示する
;	http://zip.cgis.biz/
;

; 検索する郵便番号を指定
code="1980082"	; 郵便番号
cmd = "zipcode.py " + code
cmdexec cmd, result
split result, ",", state, city, address

; 結果を出力
mes "郵便番号" + code
mes "state " + state
mes "city " + city
mes "address " + address

; プログラムの終了
end
