#include "hsp3cl.as"
#include "cmdexec.as"

sdim text, 2048, 32
; 検索する英語を指定
word = "Raspberry_Pi"
cmd = "wikipedia.py " + word
cmdexec cmd, result
split result, ",", text
text_count = stat - 1
repeat 3
    mes text(cnt)
loop


; プログラムの終了
end
