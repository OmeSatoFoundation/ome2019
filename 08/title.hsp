#include "hsp3cl.as"
#include "hspcurl.as"
#include "htmlparser.as"

; yahoo.co.jpからHTMLを取得してbufへいれる
curl "https://www.yahoo.co.jp/", buf
; バッファ変数内のtitleタグを探して上書き
htmltag buf, "title", buf
; バッファ変数を表示
mes buf
; 余計な<>を取り除いてテキストのみに
htmltext buf, "title", buf
; バッファ変数を表示
mes buf
; プログラム終了
end

