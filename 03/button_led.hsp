#include "hsp3dish.as"
#include "rpz-gpio.as"

	redraw 0
	font "",30
	pos 20,20
	mes "ボタンを押している間LEDが光るよ"
	redraw 1

*led
	btn1 = gpioin(5)
	btn2 = gpioin(6)
	if btn1==0 : gpio 18, 1 : else : gpio 18, 0
	if btn2==0 : gpio 17, 1 : else : gpio 17, 0
	await 10
	goto *led