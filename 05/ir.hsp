#include "hsp3dish.as"
#include "rpz-gpio.as"
#include "rpz-fabo.as"

BUTTON_PIN = 24
prev = 0
status=0

*main
	gpmes "ボタンPUSHで赤外線照射!"
	goto *edge

*edge
	current = gpioin(BUTTON_PIN)
	if (prev=0) & (current=1) {
		exec "irsend SEND_ONCE fan onoff"
	}
	prev = current
	update 1
	goto *edge
